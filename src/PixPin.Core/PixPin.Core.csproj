<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFrameworks>net8.0;netstandard2.1;netstandard2.0</TargetFrameworks>
    <LangVersion>latest</LangVersion>
    <Nullable>enable</Nullable>
    <GeneratePackageOnBuild>true</GeneratePackageOnBuild>
    
    <!-- Package Metadata -->
    <PackageId>PixPin.Core</PackageId>
    <PackageVersion>1.0.0</PackageVersion>
    <PackageReleaseNotes>首个正式版本发布！支持 PixPin 截图工具的完整 .NET API，包含智能进程检测、多版本支持和性能优化。详情请查看：https://github.com/QuickerHub/pixpin-sdk/releases</PackageReleaseNotes>
    <Authors>PixPin SDK Contributors</Authors>
    <Description>PixPin .NET SDK - Provides .NET methods that generate JavaScript scripts for PixPin automation</Description>
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
    <PackageProjectUrl>https://github.com/QuickerHub/pixpin-sdk</PackageProjectUrl>
    <RepositoryUrl>https://github.com/QuickerHub/pixpin-sdk</RepositoryUrl>
    <PackageTags>pixpin;screenshot;automation;sdk</PackageTags>
    <PackageReadmeFile>README.md</PackageReadmeFile>
  </PropertyGroup>

  <!-- Include README in package -->
  <ItemGroup>
    <None Include="..\..\README.md" Pack="true" PackagePath="\" />
  </ItemGroup>

  <!-- Conditional compilation for different target frameworks -->
  <PropertyGroup Condition="'$(TargetFramework)' == 'netstandard2.0' OR '$(TargetFramework)' == 'netstandard2.1'">
    <ImplicitUsings>disable</ImplicitUsings>
  </PropertyGroup>
  
  <PropertyGroup Condition="'$(TargetFramework)' == 'net8.0'">
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>

  <!-- Package references for older frameworks -->
  <ItemGroup Condition="'$(TargetFramework)' == 'netstandard2.0'">
    <PackageReference Include="System.Text.Json" Version="8.0.5" />
  </ItemGroup>
  
  <ItemGroup Condition="'$(TargetFramework)' == 'netstandard2.1'">
    <PackageReference Include="System.Text.Json" Version="8.0.5" />
  </ItemGroup>

</Project>
